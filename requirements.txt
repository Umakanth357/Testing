# Avatar Tool — Python requirements
# Install: pip install -r requirements.txt
# Note: torch installed separately in setup.sh with correct CUDA version

# Core ML
numpy>=1.24
scipy>=1.10
scikit-image>=0.21

# Computer Vision
opencv-python-headless>=4.8
Pillow>=10.0
imageio>=2.31
imageio-ffmpeg>=0.4

# Audio
soundfile>=0.12
librosa>=0.10
pydub>=0.25

# TTS
TTS>=0.22              # Coqui TTS (EN fallback)
chatterbox-tts>=0.1.0  # Chatterbox (EN primary)
# svara installed separately from git

# STT
openai-whisper>=20231117
faster-whisper>=0.10   # faster Whisper for transcription

# YouTube
yt-dlp>=2024.1.1
youtube-transcript-api>=0.6

# Face enhancement
gfpgan>=1.3.8
basicsr>=1.4
realesrgan>=0.3
facenet-pytorch>=2.5

# LLM / NLP
transformers>=4.38
accelerate>=0.27
huggingface-hub>=0.20
# ollama SDK (optional — we use HTTP API directly)
ollama>=0.1.6

# Web UI
gradio==4.44.0

# HTTP
requests>=2.31
httpx>=0.26

# Utilities
tqdm>=4.66
python-dotenv>=1.0
click>=8.1
