# ── IMPORTANT: Do NOT pip install this file directly with one command.
# ── Use setup.sh which installs in the correct order to avoid numpy conflicts.
# ── Root cause: chatterbox-tts / basicsr / realesrgan pin numpy<2; torch needs it free.
# ── Solution: pin numpy==1.26.4 FIRST, then install everything with --no-deps
#    for the problematic packages.

# ── Core ML (AMI already has PyTorch — skip torch/torchvision/torchaudio here)
transformers>=4.40.0
diffusers>=0.27.0
accelerate>=0.28.0
safetensors>=0.4.0
peft>=0.10.0

# ── Avatar Generation ─────────────────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.9.0
facexlib>=0.3.0
# gfpgan / basicsr / realesrgan installed via setup.sh with --no-deps

# ── TTS ───────────────────────────────────────────────────────────────────────
# Indic Parler-TTS: loaded via transformers/HF hub — no pip package needed
# IndicF5: loaded via transformers/HF hub — LICENSE: verify at huggingface.co/ai4bharat/IndicF5
# chatterbox-tts installed via setup.sh with --no-deps after numpy pin
TTS>=0.22.0
gtts>=2.5.0
soundfile>=0.12.0
sounddevice>=0.4.6
librosa>=0.10.0

# ── Audio Engineering ─────────────────────────────────────────────────────────
pedalboard>=0.9.0
pydub>=0.25.0
scipy>=1.11.0

# ── Video / FFmpeg ────────────────────────────────────────────────────────────
ffmpeg-python>=0.2.0
imageio>=2.33.0
imageio-ffmpeg>=0.4.9

# ── Script Engine ─────────────────────────────────────────────────────────────
openai-whisper>=20231117
faster-whisper>=1.0.0
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.0
requests>=2.31.0
httpx>=0.27.0

# ── Copyright / Similarity ────────────────────────────────────────────────────
sentence-transformers>=2.7.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
huggingface-hub>=0.22.0
tqdm>=4.66.0
psutil>=5.9.0
rich>=13.7.0

# ── UI ────────────────────────────────────────────────────────────────────────
gradio==4.44.0
